# robot-arm-cv
5-DOF robotic arm that detects and picks up objects using computer vision. ESP32-CAM streams video, OpenCV detects object position via HSV color filtering, a polynomial regression model predicts servo angles, and Arduino executes the motion via serial communication.
